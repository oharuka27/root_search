# よりみち — 設計方針

## 目的・対象

Reactで大阪市周辺の徒歩の寄り道を提案する。対象は緯度34.55〜34.85、経度135.35〜135.65。出発地・目的地、カテゴリ、公園／喫茶店／猫スポット、寄り道時間から一か所の経由地を比較する。全国対応や道路に沿う経路探索は将来拡張とする。

## 構成

- React 19 + TypeScript + Vite：日本語UI、レスポンシブ対応。
- Leaflet + OpenStreetMap：パン・ズーム、地点マーカー、概略線、地図クリックによる住所の位置指定。
- MCP TypeScript SDK v1：ブラウザの公式Client → 同一オリジンの `/mcp` → Express + Streamable HTTP。単なるRESTの名称変更ではなく、MCPの初期化・tools/callで呼び出す。
- `find_spots`：サンプルまたはOverpassの公園・カフェを取得。サーバーは住所名、履歴、正確な出発／目的地を受け取らない。必要な検索範囲を0.01度単位で外側に丸めて送信。
- ルートの比較・履歴・お気に入りの処理はブラウザ内。
- LLMや有料APIキーは不要。開発時はContext7 MCPを用いてSDK資料を確認。

## 出発地・目的地

1. 検索：収録済みの大阪の駅・公園を名前・地域で検索（外部送信なし）。任意の全国住所のジオコーディングは実装しない。
2. 自宅：名前、任意の住所文字列、位置をブラウザに保存して再利用。
3. カスタム住所：住所と名前を入力し、地図クリックまたは緯度・経度で位置を指定。
4. 指定履歴：最大20地点。重複IDを除き新しいものから表示。

住所を勝手に第三者サービスへ送信しないため、住所から座標への自動変換はしない。公開Nominatimは組み込んでいない。今後ジオコーディングを追加する場合は、自前サービスまたは契約したプロバイダーと明示的な送信説明を設計する。

## 保存・プライバシー

- 保存先は `localStorage['yorimichi.private.v1']` のみ。自宅、指定履歴、スポットIDのお気に入り、外部サービス利用設定を保存。
- サーバーDB、Cookie、ユーザーID、アクセス解析、ユーザーデータのサーバーキャッシュはない。
- localStorageはオリジンとブラウザプロファイル単位。同じブラウザプロファイルを共有する人の区別や認証・暗号化はない。別のユーザーはOSアカウントまたはブラウザプロファイルを分ける。
- 別端末／別ブラウザへ同期しない。プライベートモードや保存設定により消失・保存失敗があり得る。保存失敗・破損時は画面に通知する。
- 自宅削除、履歴削除、全保存データ削除を提供。全削除は確認後にアプリ専用キーだけを削除。現在の地点も初期化。
- 正確な座標はブラウザ内で計算に使用。MCPは大まかな範囲を受信するため、通信が完全匿名という意味ではない。
- 地図は初期状態で外部タイルを読み込まない。利用者が地図表示を有効にするとOSMへ表示範囲とIPアドレスを送信する。実データ検索には同設定を必要とする。
- Google Mapsへのリンクには出発／目的地／経由地の座標を含む。ユーザーがリンクを開く前に画面で説明する。
- サーバーはループバックで待機。MCPのOrigin・Hostを検証、リクエスト本文16KB上限、JSONスキーマで入力検証、`Cache-Control: no-store`。住所やリクエスト本文をログに出さない。
- インターネット公開時はHTTPS、認証／レート制限、プロキシのログ抑制、Host/Originの明示設定、共有端末での分離方法を追加設計する。現状はローカル実行向け。

## スポットデータ

- サンプルモードは操作確認用。公園の一部は実在地名だが、喫茶店・猫カフェは架空。必ずサンプルと表示し、営業情報として扱わない。
- 実データは利用者が検索ボタンを押した時のみOverpassから取得。公園とカフェ最大200件、タイムアウト25秒、同時リクエスト抑制と2秒間隔。検索結果の網羅性は保証しない。
- 猫スポットはカフェのタグまたは名前による猫カフェ判定。野良猫の所在地を推測して表示しない。
- 営業時間・閉店・予約要否は検証していない。OSMの情報として明示する。
- 外部サービスに失敗した際は架空データへ黙って切り替えず、エラーとサンプルへの手動切り替えを案内。
- Overpassエンドポイントは `OVERPASS_URL` で変更可能。

## 寄り道の比較

Haversineによる地点間直線距離を1.3倍したものを概算徒歩距離とする。徒歩速度4.5km/h、カテゴリごとの仮の滞在時間を用いる。
追加時間 = max(0, 経由時の概算徒歩時間 − 直行時の概算徒歩時間) + 滞在時間。
時間予算内の候補を追加時間の昇順で表示。複数経由地の最適化はしない。
地図の点線は地点を結ぶ概略で、通行可能な道や橋を保証しない。実際の徒歩道順は明示的に開くGoogle Mapsリンクで確認する。

## 検証

型検査、production build、距離・時間・カテゴリ絞り込みのユニットテスト、実MCP通信、ブラウザで検索・自宅保存・再読込・保存データ削除・モバイルレイアウトを確認する。外部APIの可用性に依存するテストは通常の自動テストに含めない。

## 参照

- [MCP TypeScript SDK v1](https://ts.sdk.modelcontextprotocol.io/)
- [MCPサーバー](https://ts.sdk.modelcontextprotocol.io/server)
- [Leaflet Quick Start](https://leafletjs.com/examples/quick-start/)
- [OpenStreetMapタイル利用方針](https://operations.osmfoundation.org/policies/tiles/)
- [Nominatim利用方針（採用していない）](https://operations.osmfoundation.org/policies/nominatim/)
- [Overpass API](https://dev.overpass-api.de/overpass-doc/en/)
